angular.module('uguru.shared.services')
    .factory('ViewService', [
    '$timeout',
    '$compile',
    '$parse',
    '$rootScope',
    ViewService
    ]);

function ViewService($timeout, $compile, $parse, $rootScope) {
    return {}
}